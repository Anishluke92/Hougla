
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
   <%= link_to "Hougla", root_path , class:" navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to "Home", root_path , class:" nav-link active" %>
        </li>
        <% if current_user %>
        <li class="nav-item">
            <%= link_to "Projects", projects_path , class:" nav-link active" %>
        </li>
        <% end %>
        <li class="nav-item">
         <a class="nav-link active" href="#">About us</a>
        </li>
      </ul>
         <ul class= "navbar-nav ms-auto mb-2 mb-lg-0">
        <% if current_user %>
          <span class="navbar-brand mb-0 h1"><%= current_user.email %> </span>  <%= button_to "Sign Out", destroy_user_session_path, method: :delete %>
        <%  else %>
          <%= button_to "Login In", new_user_session_path %>
          <%= button_to  "Sign Up", new_user_registration_path %>
        <% end %>
        </ul>

    </div>
  </div>
</nav>
